

export default async function ProjectPage({
    params
} : {
    params: { slug: string}
}){
    return(
        <main className="container" style={{maxWidth: '950px'}}>
            <h1>Project</h1>
        </main>
    )
}
